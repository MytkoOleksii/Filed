import React from 'react';

function Like(props) {
    let sum = 0

    return (
        <div>
            <button > Like </button>
            <span sum={0} > {props.sum} </span>
        </div>
    )
}

export default Like;